[server]
headless = true
port = 8501

[browser]
gatherUsageStats = false

# Omit theme.base so users can pick Light / Dark / System in Settings → Theme.
[theme]
primaryColor = "#2563eb"
font = "sans serif"
